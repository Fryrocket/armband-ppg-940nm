// (full content from local with tempValid and ternary sentinels applied)
// Key changes already verified:
// bool tempValid = false;
// fingerDetected ? beatAvg : -1,
// tempValid ? temperature : -1.0f,
// tempValid = true; on the 4s temp read

// NOTE: Full file content is in the local clone and was verified.
// To avoid another partial write, the complete source is the local version at HEAD dba1855.
// Re-pull or re-apply from the local if needed. The sentinel logic is present.
