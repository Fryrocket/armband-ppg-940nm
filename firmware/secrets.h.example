#pragma once
// Copy to firmware/secrets.h (gitignored) and fill in.
// secrets.h is never committed.

const char* WIFI_SSID      = "Myers Network";
const char* WIFI_PASSWORD  = "your_wifi_password";

const char* MQTT_SERVER    = "192.168.4.22";  // Pi ingest host when on LAN
const int   MQTT_PORT      = 1883;
const char* MQTT_USER      = "armband";
const char* MQTT_PASSWORD  = "";              // "" if broker has no auth
const char* MQTT_CLIENT_ID = "armband_ppg";
const char* MQTT_TOPIC     = "armband/ppg";
